name: 'Olá Mundo Docker Action'
description: 'Diga Olá para o usuário ou ao mundo.!'
inputs:
  who-to-greet:
    description: 'Quem cumprimenta?'
    required: true
    default: 'mundo'
outputs:
  time:
    description: 'A hora que vc disse olá'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.who-to-greet }}
